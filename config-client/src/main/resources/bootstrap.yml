# bootstrap配置优先于application配置文件 配置不会被覆盖
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
server:
  port: 8882