# bootstrap配置优先于application配置文件 配置不会被覆盖
spring:
  cloud:
    config:
      label: master
      profile: dev
      # uri: http://localhost:8888/
      discovery:
        enabled: true
        serviceId: config-server